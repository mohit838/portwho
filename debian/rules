#!/usr/bin/make -f

%:
	dh $@

override_dh_auto_install:
	:

override_dh_install:
	install -d debian/portwho/usr/bin
	cp bin/portwho debian/portwho/usr/bin/portwho

override_dh_installman:
	dh_installman man/portwho.1

override_dh_fixperms:
	:
